# OWNER_DECISIONS_REQUIRED

## Re-inspection note
Prior governance decision **OWNER-004** (implement `trade_temporal_telemetry`, refactor `scan_scheduler`) was re-inspected. Evidence still supports the prior decision; no new owner action is required to confirm that gap pair.

## New owner decisions genuinely required
| OWNER-ID | Question | Affected findings | Options | Consequences | Recommended option | Why approval is required | Blocks remediation? |
|---|---|---|---|---|---|---|---|
| OWNER-001 | Which parameter contract is authoritative for live runtime: the `strategy_v2` / `score_thresholds` schema or the current `algo_params.json` / `params_loader` schema? | GAP-004, CON-007, BATCH-02 | A) migrate runtime to current config shape; B) migrate config/loader/admin to `strategy_v2` shape; C) define third reconciled shape and migrate all layers | Without a decision, any code fix risks breaking operator expectations or canon wording | **B** — align config/loader/admin to the runtime strategy fields already implied by `params_schema.json` and `strategy_v2.py`, then update canon/docs as needed | Canon does not resolve which of the currently divergent runtime contracts should win | **Yes** |
| OWNER-002 | Should the older `core/bot_service.py` callback/panel control plane be retired in favor of `admin_commands.py` + `admin_permissions.py`, or should both be canonized and reconciled? | GAP-007, GAP-013, CON-006, CON-013 | A) retire legacy panel path; B) keep both and fully reconcile; C) keep callback panel only for docs viewer/status | Dual control planes are the main source of security and state drift | **A** — keep a single authoritative control plane | Canon requires a coherent control plane, but retiring a user-facing path is a product/operational decision | **Yes** |
| OWNER-003 | Which path family should remain canonical for mutable runtime state/config: root-level `/opt/binarybot/*.json` compatibility paths or segmented `/opt/binarybot/config|state|outcomes|observability/...` paths? | GAP-003, GAP-018, BATCH-01, BATCH-06 | A) segmented paths only; B) preserve root-level compatibility; C) support both indefinitely | Path choice affects migration, rollback, admin tooling, and state recovery | **A** — converge on segmented directories and add migration shims only during transition | Codebase contains conflicting live assumptions; choosing the target path model is operationally meaningful | **Partially** |
